name: Update base images in Dockerfiles
description: Update base images in Dockerfiles
author: "Piotrekkr"

inputs:
  dockerfile:
    description: Dockerfile path
    required: true

runs:
  using: node20
  main: dist/index.js
